# .env - Development Environment Variables (DO NOT COMMIT TO GIT!)

# Core Django Settings
DEBUG=True
SECRET_KEY=django-insecure-(_kijww=(-u08b-&jv
#7vi35ghj1amjh^9-swh)dtbn$c_v9bf # Your current insecure key for local dev
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,.ngrok.io 
# Add any other dev hosts, e.g., ngrok tunnels

# Database Settings for Local Development (MySQL)
# Format: mysql://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL=mysql://root:Pass@123@localhost:3306/ubc_db

# CORS Settings for Local Development
# CORS_ALLOWED_ORIGINS is generally preferred over CORS_ALLOW_ALL_ORIGINS in production
# For dev, you can list specific origins if needed, but CORS_ALLOW_ALL_ORIGINS=True is common.
# If CORS_ALLOW_ALL_ORIGINS=True, then CORS_ALLOWED_ORIGINS is ignored by django-cors-headers.
# So, you can remove CORS_ALLOWED_ORIGINS=[] from settings.py and handle CORS_ALLOW_ALL_ORIGINS with env.
CORS_ALLOW_ALL_ORIGINS=True

# JWT Settings
SIMPLE_JWT_ACCESS_TOKEN_LIFETIME_DAYS=30
SIMPLE_JWT_REFRESH_TOKEN_LIFETIME_DAYS=365
 # Good for mobile apps to reduce re-login

# Custom Admin URL (good practice even for dev to train for prod)
ADMIN_URL=admin/

# Logging (for development, logs to console and a local file)
LOG_FILE_PATH=./logs/django_debug.log 
# Path relative to BASE_DIR